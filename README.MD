To Use This theme on a local machine:

First clone the theme via ssh:
```
git clone git@github.com:szareui/portio-szare.git
```
Then use these commands:

```
git submodule init
git submodule update
```